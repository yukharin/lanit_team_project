import { NotificationStatusPipe } from './notification-status.pipe';

describe('NotificationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
