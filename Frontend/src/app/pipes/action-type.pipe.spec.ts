import { ActionTypePipe } from './action-type.pipe';

describe('ActionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ActionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
